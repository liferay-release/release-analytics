# analyze_build

Placeholder for future build-analysis tooling. Empty today.

Reserved for scripts that inspect a built Liferay DXP bundle — module
inventory, dependency diffs against prior release, binary-level change
detection — intended to live alongside `bundle_validation/` but separate
because "analyze" is a longer-running investigation, not a pass/fail gate.
